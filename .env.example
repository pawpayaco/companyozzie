# === Supabase ===
SUPABASE_URL=https://your-project.supabase.co                # ✅ Your Supabase project URL
SUPABASE_ANON_KEY=your-anon-key-here                         # ✅ Public client key (used on frontend)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key-here         # ✅ Secret server key (used in API calls)

# === Plaid (optional for ACH) ===
PLAID_CLIENT_ID=your-plaid-client-id                         # 🔍 Only used if ACH payouts get built
PLAID_SECRET=your-plaid-secret
PLAID_ENV=sandbox

# === Dwolla ===
DWOLLA_KEY=your-dwolla-key                                   # 🔍 For future payout routing
DWOLLA_SECRET=your-dwolla-secret
DWOLLA_ENV=sandbox

# === Tapify === (💡 Rename if you want)
NEXT_PUBLIC_BASE_URL=https://yourdomain.com                  # ✅ Used for generating full links
NEXT_PUBLIC_TAPIFY_BRAND_NAME=Tapify                         # 🔍 Used in UI (you can change to "Pawpaya")

# === Admin ===
ADMIN_EMAIL=admin@example.com                                # 🔍 Used to give admin access in /admin.js

# === Optional Defaults ===
DEFAULT_VENDOR_COMMISSION=50                                 # ✅ Used as fallback % payout
DEFAULT_RETAILER_COMMISSION=20
DEFAULT_SOURCER_COMMISSION=10
